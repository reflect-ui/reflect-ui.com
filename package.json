{
  "name": "reflect-ui.com",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "web": "yarn workspace web next dev",
    "git:update": "git submodule update --init --recursive"
  },
  "workspaces": [
    "web",
    "packages/*"
  ]
}